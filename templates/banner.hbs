{{!-- WoD Pretty Theme — custom footer banner (Glass Gótico), rebuilt from
      scratch as a clean, in-flow bar (no sticky/absolute overlap). Shows the
      splat label centered with wide tracking, like the design footer. --}}
{{#if displayBanner}}
    <div class="gg-footer">{{localize currentTypeLabel}}</div>
{{/if}}
